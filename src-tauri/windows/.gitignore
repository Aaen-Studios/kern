# skin.zip is regenerated by make-installer-skin.mjs from the skin/ assets.
# Keep the PNGs/XML committed so @resvg/resvg-js isn't required for a clean
# checkout — only re-run installer:assets when the palette or logo changes.
nsis-skin/skin.zip
